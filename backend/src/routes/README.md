# Carpeta `routes`

En esta carpeta se definirán las rutas de la aplicación, asociando las URLs con los controladores correspondientes.

Por ejemplo: una ruta de usuarios puede definir que al acceder a `GET /users` se ejecute el controlador encargado de listar todos los usuarios.
