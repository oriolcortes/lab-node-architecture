# Carpeta `interfaces`

En esta carpeta se almacenarán las interfaces TypeScript utilizadas para definir la estructura de los datos y tipos en la aplicación.

Por ejemplo: una interfaz `User` que define las propiedades que debe tener un usuario, como `id`, `nombre`, `email`, etc.
