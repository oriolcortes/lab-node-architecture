# Carpeta `middlewares`

En esta carpeta se almacenarán los middlewares de la aplicación, que permiten interceptar y procesar las solicitudes HTTP antes de llegar a los controladores.

Por ejemplo: un middleware puede verificar si el usuario está autenticado antes de permitir el acceso a ciertas rutas, o validar el formato de los datos recibidos en una solicitud.
